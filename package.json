{
  "name": "@tokenbound/contracts",
  "version": "0.3.1",
  "scripts": {
    "compile": "forge test",
    "format": "forge fmt",
    "lint": "solhint -w 0 'contracts/**/*.sol'",
    "prepublishOnly": "echo 'ERROR: Use script/publish.sh to publish' && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tokenbound/contracts.git"
  }
}
